CMAKE_MINIMUM_REQUIRED(VERSION 3.0...3.20)
PROJECT(ModbusThings)



INCLUDE_DIRECTORIES(${TD_COMMUNITY_DIR}/src/deps/cJson/inc)


INCLUDE_DIRECTORIES(inc)
AUX_SOURCE_DIRECTORY(src SRC)

ADD_LIBRARY(core ${SRC})
TARGET_LINK_LIBRARIES(core mutils cJson os libcurl port ${LINK_MODBUS} ${LINK_MQTT})
